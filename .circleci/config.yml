version: 2.1

jobs:
  hello_world:
    docker:
      - image: ubuntu:latest
    steps:
      - run:
          name: Print Hello World
          command: echo "Hola mundo"

workflows:
  version: 2
  hello_workflow:
    jobs:
      - hello_world
